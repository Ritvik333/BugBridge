server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: "bugboard1@gmail.com"  # Sender's email (not the user's)
    password: "coqx yphh dkxr rzgc"  # App-specific password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  thymeleaf:
    prefix: file:../frontend/templates/
    suffix: .html
    cache: false
  web:
    resources:
      static-locations: file:../frontend/static/
      add-mappings: true
  datasource:
    password: root
    url: jdbc:mysql://127.0.0.1:3306/user_reg?reconnect=true
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true